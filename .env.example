# Anthropic API key used for operator-note interpretation (required).
ANTHROPIC_API_KEY=

# Model to use for interpretation (optional, defaults shown).
ANTHROPIC_MODEL=claude-sonnet-4-5

# Per-call LLM timeout in seconds (optional, default 20).
LLM_TIMEOUT_SECONDS=20
